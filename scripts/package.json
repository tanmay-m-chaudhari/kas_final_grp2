{
  "name": "logstream-scripts",
  "version": "1.0.0",
  "description": "Internal build and codegen scripts for LogStream",
  "scripts": {
    "generate:schema": "node src/generateSchema.js",
    "lint:routes": "node src/lintRoutes.js"
  },
  "dependencies": {
    "glob": "^10.3.10",
    "chalk": "^5.3.0",
    "js-yaml": "^4.1.0"
  }
}
